#include <bits/stdc++.h>

using namespace std;

int main() {

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    short int testes, n;
    string bin, sim;

    cin >> testes;
    for(int i = 0; i < testes; i++)
    {
        cin >> n;
        cin >> bin;

        for(int j = 0; j < n; j++)
        {
            if(bin[j] == '1') {
                
            }
        }
    }



    return 0;
}